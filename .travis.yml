language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t dusanbolek/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dusanbolek/docker-react npm run test
